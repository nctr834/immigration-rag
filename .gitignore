# Secrets
.env

# Python
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Data — raw PDFs can be large; keep the folder, ignore the contents.
# Derived/extracted text sources are small and diffable, so track those.
data/*
!data/.gitkeep
!data/*.txt

# Eval outputs
eval/results/

# OS / editor
.DS_Store
.idea/
.vscode/
